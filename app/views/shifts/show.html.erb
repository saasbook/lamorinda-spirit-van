<p style="color: green"><%= notice %></p>

<% content_for :title, "Shift Details" %>

<%= render @shift %>

<div>
  <%= link_to "Edit this shift", edit_shift_path(@shift) %> |
  <%= link_to "Back to shifts", shifts_path %>

  <%= button_to "Destroy this shift", @shift, method: :delete %>
</div>
