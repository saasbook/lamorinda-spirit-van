<p style="color: green"><%= notice %></p>

<% content_for :title, "Shift Details" %>

<%= render @shift %>



<%= render "shift_rides" %>

<div class="text-right mt-4 mb-3">
  <%= link_to "Add new ride", new_ride_path(date: @shift.shift_date, driver_id: @shift.driver), class: "btn btn-primary" %>
</div>


<div class="mb-3">
  <%= form_with model: @shift do |form| %>
    <%= form.label :notes, "Notes about Shift/Timeline:" %>
    <%= form.text_area :notes, class: "form-control", value: @shift.notes, rows: 8 %> 
    <div class="text-center mt-2">
      <%= form.submit "Save", class: "btn btn-success py-2" %>
    </div>
  <% end %>
</div>

<div class="d-flex justify-content-center gap-2 mt-4">
  <%= link_to "Edit this shift", edit_shift_path(@shift), class: "btn btn-primary" %>
  <%= link_to "Shift Feedback", feedback_shift_path(@shift), class: "btn btn-primary" %>
  <%= link_to "Back to shifts", request.referer, class: "btn btn-secondary" %>
  <%= button_to "Delete this shift", @shift,
        method: :delete,
        class: "btn btn-danger",
        data: { turbo_confirm: 'Are you sure?' }
  %>
</div>
