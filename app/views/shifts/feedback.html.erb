<% content_for :title, "Shift Feedback" %>

<h1>Feedback for <%= @shift.shift_type %> shift on <%= @shift.shift_date %></h1>

<%= form_with(model: @shift) do |form| %>
  <% if @shift.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@shift.errors.count, "error") %> prohibited this shift from being saved:</h2>

      <ul>
        <% @shift.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :van, "Van: ", style: "display: block" %>
    <%= form.number_field :van, required: false, value: @shift.van %>
    <%= form.label :pu_time, "Van pick up time from base: ", style: "display: block" %>
    <%= form.text_field :pu_time, required: false, value: @shift.pu_time %>
    <%= form.label :do_time, "Van drop off time from base: ", style: "display: block" %>
    <%= form.text_field :do_time, required: false, value: @shift.do_time %>
    <%= form.label :odo_pre, "Odometer reading pre-trip: ", style: "display: block" %>
    <%= form.text_field :odo_pre, required: false, value: @shift.odo_pre %>
    <%= form.label :odo_pst, "Odometer reading post-trip: ", style: "display: block" %>
    <%= form.text_field :odo_pst, required: false, value: @shift.odo_pst %>
  </div>

  <div>
    <%= form.button "Save Feedback", type: :submit, name: "commit_type", value: "feedback" %>
  </div>
<% end %>

<br>

<div>
  <%= link_to "Show this shift", @shift %> |
  <%= link_to "Back to today", today_driver_path(id: @shift.driver_id) %>
</div>
